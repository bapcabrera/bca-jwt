@{
    Module = @{
        Describe     = "Module"
        ImportModule = "Importation locale du module."
        CommandCheck = "Vérification du nombre de fonctions exportées."
    }

    Token  = @{
        Describe           = "Token"
        New                = "Création d'un token"
        NewHS256           = "Création d'un token (HS256)"
        NewHS256Claims     = "Création d'un token (HS256 depuis des demandes)"
        NewRS256Cert       = "Création d'un token (RS256 depuis un certificat)"
        NewRS256PrivateKey = "Création d'un token (RS256 depuis une clé privée)"
        ConvertFrom        = "Conversion d'un token en object"
        ConvertTo          = "Conversion d'un object en token"
    }
}